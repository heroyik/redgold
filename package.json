{
  "name": "redgold",
  "version": "1.0.3",
  "description": "Mobile-First Chinese Vocabulary App",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "version:bump": "node scripts/bump-version.mjs",
    "vocab:extract": "node scripts/extract-vocab.mjs",
    "vocab:validate": "node scripts/validate-vocab.mjs"
  },
  "dependencies": {
    "firebase": "^10.0.0",
    "pinyin-pro": "^3.28.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "dotenv": "^17.4.2",
    "firebase-admin": "^13.8.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
